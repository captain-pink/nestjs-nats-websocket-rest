export enum MongoVariables {
  USERNAME,
  PASSWORD,
  HOST,
  PORT,
  DB,
  PROTOCOL,
}

export enum NatsVariables {
  HOST,
  NAME,
  PORT,
  TRANSPORT,
  PROTOCOL,
}

export enum WsVariables {
  PORT,
}

export enum ApiVariables {
  PORT,
  NAME,
  HOST,
  PROTOCOL,
}
